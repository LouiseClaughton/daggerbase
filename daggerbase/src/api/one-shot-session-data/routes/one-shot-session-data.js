'use strict';

/**
 * one-shot-session-data router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::one-shot-session-data.one-shot-session-data');
