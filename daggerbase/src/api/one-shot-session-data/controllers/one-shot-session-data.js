'use strict';

/**
 * one-shot-session-data controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::one-shot-session-data.one-shot-session-data');
